# semantically-guided-video-compression
Video compression for finding a subset of frames which maintain semantic meaning.  Assumes posed rgb frames as an input.  Applications vary, but efficient memory for robots over long time horizons (hours, days, months) is the motivation. 
